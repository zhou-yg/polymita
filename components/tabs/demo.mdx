import { useState } from 'react'
import * as Module from './index.tsx'
import * as TestModule from './testingTabs.tsx'
import { RenderToReactWithWrap } from '../../shared/render'
export const Component = RenderToReactWithWrap(Module)
export const TestingComponent = RenderToReactWithWrap(TestModule)

# Tabs 标签

头部标签

<Component
  tabs={['tab1', 'tab2', 'tab3']}
/>

头部props内容

<Component
  tabs={['tab1', 'tab2', 'tab3']}
  panels={[
    <strong>strong</strong>,
    <bold>strong</bold>,
    <del>strong</del>,
  ]}
/>

tabs内容

<TestingComponent />