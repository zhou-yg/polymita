import * as TestModule from './index-test.tsx'
import * as FormModule from './index.tsx'
import { RenderToReactWithWrap } from '../../shared/render'
import { useState } from 'react'
export const Component = RenderToReactWithWrap(TestModule)
export const FormComponent = RenderToReactWithWrap(FormModule)
export function TestCase () {
  const [val, setVal] = useState('v0')
  return (
    <div style={{ margin: '10px' }}>
      <Component />
    </div>
  )
}


export function TestCase2 () {
  const [values, setValues] = useState({
    first: '',
    second: '',
  });
  return (
    <div style={{ margin: '10px'  }}>
      <div>
      {JSON.stringify(values)}
      </div>
      <FormComponent
        onChange={v => {
          console.log('v:', v);
          setValues(pre => Object.assign({}, pre, v))
        }}
        form={[
          {
            name: 'first',
            value: values.first
          },
          {
            name: 'second',
            value: values.second
          },
        ]}
      />
    </div>
  )
}

# Form 表单

普通 Form Input 表单

<TestCase />

单独使用 Form 组件

<TestCase2 />