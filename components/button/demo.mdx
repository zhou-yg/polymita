import * as Button from './index.tsx'
import { RenderToReact } from '../../shared/render'
export const RButton = RenderToReact(Button)

# Button 按钮

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑

<RButton pattern={{ type: 'primary' }} >pattern is primary</RButton>

基本的按钮展示
